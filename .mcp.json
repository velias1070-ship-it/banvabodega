{
  "mcpServers": {
    "github": {
      "command": "sh",
      "args": [
        "-c",
        "GITHUB_PERSONAL_ACCESS_TOKEN=\"$(cat ~/.config/claude/github-token)\" exec npx -y @modelcontextprotocol/server-github"
      ]
    }
  }
}
